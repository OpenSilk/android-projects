<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="org.opensilk.music">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MusicApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="org.opensilk.music.ui.activities.HomeSlidingActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.FolderSlidingActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".ui.activities.HomeSlidingActivity"/>

        <provider
            android:authorities="${applicationId}.provider.index"
            android:name=".data.MusicProvider"/>
        <service android:name=".data.ScannerService"/>
        <receiver android:name=".data.ScannerConnectivityReceiver">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>