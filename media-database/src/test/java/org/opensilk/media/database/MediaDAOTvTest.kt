package org.opensilk.media.database

import org.assertj.core.api.Assertions
import org.junit.Test

/**
 * Created by drew on 8/13/17.
 */
class MediaDAOTvTest {
    /*
    @Test
    fun tv_series_returns_same_as_added() {
        val series = tvSeries()
        mClient.addTvSeries(series)
        Assertions.assertThat(mClient.getTvSeries(series.id).blockingGet()).isEqualTo(series)
    }

    @Test
    fun tv_epiode_returns_same_as_added(){
        val episode = tvEpisode()
        mClient.addTvEpisodes(listOf(episode))
        Assertions.assertThat(mClient.getTvEpisode(episode.id).blockingGet()).isEqualTo(episode)
    }
    */
}